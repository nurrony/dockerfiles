## Maven

The maven directory contains Maven-based Java build images.

The current Maven image includes:

- Maven 3.8.x
- Amazon Corretto 17


Dockerfiles are versioned by filename so different Java/Maven combinations can be maintained independently.
